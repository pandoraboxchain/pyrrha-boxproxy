{
  "name": "pandora-explorer-backend",
  "version": "1.0.0",
  "description": "API Pandora Blockexplorer",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx babel ./app --out-dir ./app-compiled --presets env --source-maps",
    "start": "npm run build && node ./app-compiled/app.js",
    "dev": "nodemon --delay 3 --watch ./app --watch ./config --watch ./pandora-abi --exec npm run start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "web3": "^1.0.0-beta.29",
    "websocket": "1.0.24"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "nodemon": "^1.12.1",
    "truffle-contract": "^3.0.0"
  }
}
